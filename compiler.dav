ah is_letter(c) {
    return (c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z') || (c == '_');
}

ah is_digit(c) {
    return c >= '0' && c <= '9';
}

ah is_space(c) {
    return c == ' ' || c == '\n' || c == '\t';
}


ah check_keywords(s, i, kw) {
    beg x = 0;
    boo(x);
    return x;
}


ah main() {
    beg x = 0;
    while x < 5 {
        boo(x);
        x = x + 1 * 3;
    }
}
